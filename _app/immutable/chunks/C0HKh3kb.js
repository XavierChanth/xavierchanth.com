import{a,f as n}from"./1kz9IMli.js";import"./AWc5D6On.js";import{Q as i,R as l,S as r,T as h,P as d}from"./Cbs7v_Ax.js";import{h as p}from"./DM2HiOdw.js";const c={title:"AI Riffraff, Part 2: Context Should Be Lazy",description:"A case for lazy context, small AGENTS.md files, and using plain Markdown instead of skill sprawl.",author:"Xavier Chanthavong",date:"2026-05-14",published:!0},{title:g,description:w,author:b,date:v,published:I}=c;var u=n('<h2>Hot Take: Skills Are Usually Bad</h2> <p>Okay, this is sort of a hot take, but I think skills are usually a bad way to use AI in a large project, especially in a monorepo.</p> <p>It is not just that skills are misused. I think they are often misauthored, and the difference matters. Even for skills that are eval’d properly, where someone has written a bunch of evals to make sure the skill guides the model toward the correct behavior, I have a hunch that most of those evals do not care about token churn.</p> <p>They probably test the positive case.</p> <blockquote><p>Does the skill help when it is relevant?<br/> Does the model follow the right workflow?<br/> Does the answer improve when this skill is loaded?</p></blockquote> <p>Those are all good questions, but I would assume most evals are not testing the negative case.</p> <blockquote><p>Does the skill stay out of the way when it is irrelevant?<br/> Does it avoid triggering when it should not?<br/> How much context does it add to tasks that do not need it?</p></blockquote> <p>This is something worth thinking about when authoring skills. A skill can be helpful in the right moment and still be bad tooling overall if it creates context inflation everywhere else.</p> <h2>The Hidden Cost: Context Churn</h2> <p>Skill descriptions are hard to write well.</p> <p>They cannot be too short, because then the agent will not have enough information to know when the skill should be used. But they also cannot be too long, because the description itself is part of the context the agent has to carry around. That tradeoff is not a big deal with one skill.</p> <p>The problem starts when you compound it across a project. One description may not affect you much. Twenty skills means twenty descriptions. Even if every single one of those descriptions is reasonable on its own, together they start to add up and eat into the context window.</p> <p>This is especially true in monorepos. A monorepo usually has multiple projects, multiple stacks, multiple workflows, and a lot more tooling. Each area can justify having some kind of guidance. Each tool can justify its own skill. But once everything gets a skill, the discovery layer itself becomes expensive.</p> <p>That is the context churn I care about. Skill authors often seem to assume that because the project uses a tool, the skill for that tool is always relevant. That is not true.</p> <p>If I am changing a CSS class in a Svelte component, I do not want database deployment guidance in context. If I am refactoring a Rust package, I do not want frontend testing instructions unless they are actually relevant. The repo can use all of those tools, but the task usually needs only a small slice of them.</p> <h2>A Real-World Example: Convex’s AI Files</h2> <p>I have been using Convex frequently, and I want to be clear that I do like that they are putting effort into this space. I am a Convex customer, and overall I like what they are doing.</p> <p>But I really do not like the way they have done their AI files. The first issue is that they try to add guidance to both <code>AGENTS.md</code> and <code>CLAUDE.md</code>. I do not use Claude. Nobody touching this project is going to use Claude. I do not want a <code>CLAUDE.md</code> file automatically generated in my project every time the AI files update. Nor do I want them touching <strong>my</strong> AGENTS.md file and filling it with junk.</p> <p>The second issue is the skills.</p> <p>Convex has a built-in router skill as part of its AI files. That skill is designed to tell the model how to find the other Convex skills. I think that is the right general shape. The problem is that the subskills are not nested inside the router skill as additional Markdown resources. The other skills are separate top-level skills, which means all of their descriptions go into context too.</p> <p>So now we have a router skill that exists to tell the model when to load the other skills, but the descriptions for those other skills are already being loaded for discovery. That defeats the point of the router. When I looked at this in May 2026, the subskill descriptions were longer than the router skill description itself. The whole setup ends up using far more skill-description bandwidth than it needs to. Not just 6x because there are six skills, but closer to 7x or 8x if you look at the relative length of the descriptions.</p> <p>That is what frustrates me. The idea is good, but the implementation undermines the reason to have a router skill in the first place.</p> <h2>Context Should Be Lazy</h2> <p>The broader principle here is that context should be lazy.</p> <p>This is similar to how I think about my Neovim setup. I created a wrapper around <code>vim.pack</code> so that I can control when things load into my editor.</p> <p>The goal is simple: do not load something you do not need. Defer as much of the loading process as possible to when, and if, you actually need it.</p> <p>Skills are a step in the right direction because they defer detailed instructions. That is good, but the discovery layer still has a cost. A router skill is the right idea. It gives the agent one place to decide whether more specific guidance is needed. But if all of the subskills also sit at the top-level discovery layer, then we are not really being as lazy as we could be.</p> <p>This matters most in monorepos. Most tasks only need one small part of the repo. If every tool, package, app, and workflow gets its own top-level skill, then every task starts carrying discovery context for things it does not need.</p> <h2><code>AGENTS.md</code> Is Already A Router Skill</h2> <p>The better way to think about this is that <code>AGENTS.md</code> is already an always-on skill.</p> <p>All* agent harnesses read it. That means it is always loaded into context, so it should be treated like an always-loaded router skill. (*all of the ones I care about anyways)</p> <p>It should not contain every detail about the repo. It should contain exactly enough information for the agent to go and find the information it needs. In one of my repos, my <code>AGENTS.md</code> file is around twenty lines, and seven of those lines are whitespace. So really, it is about thirteen lines of actual content.</p> <p>Here’s the whole file:</p> <pre class="language-markdown"><!></pre> <p>It points to the repo structure. It points to docs. It tells the agent to run <code>just --list</code> to find commands it might want to run. And that is basically all it needs.</p> <p>If the agent needs more information, it knows where to go. That is the job of the root file. It should be a map, not the territory.</p> <h2>Repo Knowledge Should Not Live In Skills</h2> <p>Skills are a bad place to store repo-specific knowledge, agents are far less likely to touch them compared to a docs folder.</p> <p>The beauty of thinking about <code>AGENTS.md</code> as a router skill is that it works well for both tooling and knowledge. It can point the agent toward commands, whether that is a <code>Justfile</code>, a <code>commands.md</code> file, or whatever else the repo uses. It can also point the agent towards architecture decisions, package boundaries, deployment notes, and workflow details.</p> <p>That information should still exist, but I don’t know of anyone putting it in skills, even though the discovery mechanisms for these things should be largely the same.</p> <h2>When Skills Are Worth It</h2> <p>I am not saying skills should never exist.</p> <p>I still have one global skill right now. It tells LLMs how I like to commit code with Jujutsu. That workflow earns being a skill because I use it frequently, it applies across projects, and it includes scripts that help pull the right context. It also compensates for the fact that LLMs know Git much better than jj, so I need to provide more explicit guidance.</p> <p>A skill should be broadly relevant or used frequently. If it is global, it should apply across many projects. If it is project-local, it should apply to a meaningful amount of work in that project.</p> <p>If it is low-frequency, it probably should not be a top-level skill. That does not mean the information is useless. It just means it should sit behind a thinner discovery layer, or live as Markdown that the agent can load when needed.</p> <p>The real question is not:</p> <blockquote><p>Can this skill help?</p></blockquote> <p>The real question is:</p> <blockquote><p>Is this skill worth carrying around all the time?</p></blockquote> <p>Most of the time, I think the answer is no.</p> <h2>Takeaway</h2> <p>Skills are Markdown, which makes me telling you to replace them with markdown a little funny. But they are not just Markdown. They also have frontmatter descriptions, which contain trigger and discovery behavior. All of that creates context inflation if it is authored poorly, or you have too many skills in context.</p> <p>Keep <code>AGENTS.md</code> small, and your list of skills smaller. Treat <code>AGENTS.md</code> like an always-on router skill. Point the agent toward the files it needs and have it load them only when it needs them.</p>',1);function T(o){var e=u(),t=i(l(e),66),s=r(t);p(s,()=>`<code class="language-markdown"><span class="token title important"><span class="token punctuation">#</span> AGENTS.md</span>

This is a mixed TypeScript + Rust monorepo. Bun is the default TypeScript
package manager for this project.

Top-level layout:

<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;apps/&#96;</span> — applications.
<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;components/&#96;</span> — Convex components.
<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;packages/&#96;</span> — shared Rust and TypeScript packages.

The default backend/database choice in this repo is Convex.

Useful starting points:

<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;Justfile&#96;</span> — common repo commands. Run <span class="token code-snippet code keyword">&#96;just --list&#96;</span>.
<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;repo/README.md&#96;</span> — repo-wide structure, tooling, and workflow decisions.
<span class="token list punctuation">-</span> <span class="token code-snippet code keyword">&#96;docs/README.md&#96;</span> — project design notes, including cryptography decisions.

Prefer existing conventions before adding new tools.</code>`),h(t),d(38),a(o,e)}export{T as default,c as metadata};
